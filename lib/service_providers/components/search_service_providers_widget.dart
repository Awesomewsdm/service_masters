import "package:service_masters/common/barrels.dart";

class SearchServiceProvidersWidget extends StatelessWidget {
  const SearchServiceProvidersWidget({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return IconButton(
      icon: const Icon(
        CustomIcons.search,
      ),
      onPressed: () {},
    );
  }
}
