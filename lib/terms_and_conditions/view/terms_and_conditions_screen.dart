import "package:flutter/material.dart";

class TermasAndConditions extends StatelessWidget {
  const TermasAndConditions({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("TermasAndConditions"),
      ),
      body: const Center(
        child: Text("TermasAndConditions"),
      ),
    );
  }
}
